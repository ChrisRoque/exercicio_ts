function multiplicacao(num1: number, num2: number): number {
    return num1 * num2;
};

function ola(nome:string): string {
    return nome;
};

console.log(multiplicacao(2, 3));
console.log(ola(`Olá ${'Christian'}!`));